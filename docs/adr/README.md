# ADRs

このディレクトリには Architecture Decision Record（ADR）を保存する。

- **命名規則**：`NNNN-short-kebab-case.md`
- **状態**：本文先頭に `Proposed`、`Accepted`、`Superseded` のいずれかを明記する。
- **更新規則**：実装完了後に `Proposed` から `Accepted` へ更新する。

単なる実装メモではなく、「なぜその設計判断を採用したか」という意思決定の背景と理由を記録する。
本文は日本語で記述する。
