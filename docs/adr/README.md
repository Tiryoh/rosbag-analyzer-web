# ADRs

このディレクトリには Architecture Decision Record を保存する。

- 命名規則: `NNNN-short-kebab-case.md`
- 例: `0001-reindex-warning-classification.md`
- 状態は本文先頭に `Proposed` / `Accepted` / `Superseded` のいずれかで記録する
- 実装完了後に `Proposed` から `Accepted` へ更新する

軽い実装メモではなく、`なぜその設計判断を採ったか` を残したい場合に使う。
日本語で記載する。
